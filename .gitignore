.env
__pycache__/
*.pyc
.venv/
venv/

# Runtime data lives in the OS user data directory. /data is only the
# pre-0.1 layout, kept locally as the source for `swingdash migrate-legacy`.
/data/

# Textual screenshots (saved from the running TUI)
Live_RVOL_*.svg

# Tooling
.pytest_cache/
.ruff_cache/
.pyright/
build/
dist/
*.egg-info/
